#!/usr/bin/env bash
{ set +x; } 2>/dev/null

! [ -d python/packages ] && echo "ERROR: ${PWD}/python/packages/ not exists" && exit 1

while read -r pkg_dir; do
    ( set -x; python -m python_package_pyproject_gen "$pkg_dir" ) || exit
done < <(find python/packages -type d -mindepth 1 -maxdepth 1)
