#!/usr/bin/env bash
{ set +x; } 2>/dev/null

! [ -d python/packages ] && echo "ERROR: ${PWD}/python/packages/ not exists" && exit 1

# celery
( set -x; repo-celery-init-py-gen ) || exit
# django
( set -x; repo-django-managers-init-py-gen ) || exit
( set -x; repo-django-models-init-py-gen ) || exit
( set -x; repo-django-schemas-init-py-gen ) || exit
( set -x; repo-django-serializers-init-py-gen ) || exit
( set -x; repo-django-querysets-init-py-gen ) || exit
( set -x; repo-django-utils-init-py-gen ) || exit
