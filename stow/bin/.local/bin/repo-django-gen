#!/usr/bin/env bash
{ set +x; } 2>/dev/null

! [ -d python/packages ] && echo "ERROR: ${PWD}/python/packages/ not exists" && exit 1

[ -d sql ] && { ( set -x; repo-django-models-gen ) || exit; }
( set -x; repo-django-contracts-gen ) || exit
